# Drop a cryptiq.db here to preseed the SQLite demo with a cached scan.
# This directory is mounted read-only at /seed in the backend container.
# The .db file itself is git-ignored; only this placeholder is tracked.
