# Cryptiq backend — fully pinned runtime dependencies.
#
# These versions are the exact set the project is verified against (759 passed /
# 34 skipped, ruff clean). The Docker image installs from this file so builds
# are reproducible regardless of when they run. Regenerate deliberately, never
# by floating the pins.
#
# Direct dependencies mirror pyproject.toml [project.dependencies]; the rest are
# their locked transitive closure.

# --- web framework / server ---
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
h11==0.16.0
httptools==0.8.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.1.1
click==8.5.0
anyio==4.15.1
sniffio==1.3.1

# --- settings / validation ---
pydantic==2.13.5
pydantic_core==2.46.5
pydantic-settings==2.15.0
annotated-types==0.8.0
annotated-doc==0.0.5
typing_extensions==4.16.0
typing-inspection==0.4.4
python-dotenv==1.2.3

# --- database / migrations ---
SQLAlchemy==2.0.52
alembic==1.19.2
Mako==1.4.1
MarkupSafe==3.0.3
# PostgreSQL driver (unused on SQLite; required for the Postgres deployment path)
psycopg[binary]==3.2.10

# --- HTTP client (GitHub ingestion) ---
httpx==0.28.1
httpcore==1.0.9
certifi==2026.7.22
idna==3.19
charset-normalizer==3.5.1
urllib3==2.7.0
requests==2.34.2

# --- Gemini explanation layer ---
google-genai==2.22.0
google-auth==2.57.1
pyasn1==0.6.4
pyasn1_modules==0.4.2
tenacity==9.1.4
distro==1.9.0
cffi==2.1.1
pycparser==3.0
cryptography==50.0.1

# --- misc transitive ---
PyYAML==6.0.3
packaging==26.3
Pygments==2.21.0
